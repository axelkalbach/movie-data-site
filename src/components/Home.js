import SearchBar from "./SearchBar";
// import { Outlet } from "react-router-dom";

function Home() {
	return (
		<div className="Home">
			<SearchBar />
			{/* <Outlet /> */}
			home
		</div>
	);
}

export default Home;
